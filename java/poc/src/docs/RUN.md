## How to run

```bash
java -version
./gradlew --refresh-dependencies
./gradlew clean build
./gradlew build
./gradlew bootRun
```


## Using maven
```bash
mvn -version
mvn clean package
mvn spring-boot:run
```

## Or run output .jar

- `java -jar target/your-app.jar`